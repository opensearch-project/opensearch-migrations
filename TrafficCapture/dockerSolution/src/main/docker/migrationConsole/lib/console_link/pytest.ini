[pytest]
markers =
    slow: tests dependent on creating containers

# Logging configuration
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)5s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Capture logs on failure
log_file_level = INFO
