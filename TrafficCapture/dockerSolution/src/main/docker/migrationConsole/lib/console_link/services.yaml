source_cluster:
  endpoint: "http://opensearchsource:9200"
  no_auth:
  # allow_insecure: true
  # basic_auth:
  #     username: "admin"
  #     password: "admin"
target_cluster:
  endpoint: "http://opensearchtarget:9200"
  no_auth:
  # allow_insecure: true
  # basic_auth:
  #     username: "admin"
  #     password: "myStrongPassword123!"
metrics_source:
  prometheus:
    endpoint: "http://prometheus:9090"
backfill:
  reindex_from_snapshot:
    docker:
snapshot:
  snapshot_name: "snapshot_2023_01_01"
  fs:
    repo_path: "/snapshot/test-console"
metadata_migration:
  from_snapshot:
  min_replicas: 0
