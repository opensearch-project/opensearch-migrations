{
  "name": "@opensearch-migrations/migration-workflow-templates",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsgo && tsc-alias",
    "make-templates": "tsx makeTemplates.cjs",
    "pretype-check": "npm run -w @opensearch-migrations/schemas build && npm run -w @opensearch-migrations/argo-workflow-builders build",
    "type-check": "npx tsgo --noEmit",
    "test": "jest --config ./jest.config.js"
  },
  "dependencies": {
    "@opensearch-migrations/argo-workflow-builders": "*",
    "@opensearch-migrations/schemas": "*",
    "@kubernetes/client-node": "^1.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20250919.1",
    "tsconfig-paths": "*"
  },
  "devDependencies": {
    "@types/jest": "*",
    "expect-type": "*"
  }
}
