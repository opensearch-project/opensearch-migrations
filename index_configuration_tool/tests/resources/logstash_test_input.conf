input {

    plugin1 {
        string_key => "string value"
        bool_key   => true
        num_key    => 1
    }

    plugin2 {
        neg_key   => -1
    }

    elasticsearch {
        hosts     => ["host1", "host2"]
        user      => "test_user"
        password  => "password"
    }
}

filter {
    plugin3 {
        str_array => ["abc", "x y z"]
    }
}

output {
    plugin4 {}

    plugin5 {
        num_array => [0]
    }

    opensearch {
        hosts     => "os_host"
        ssl       => "true"
        user      => "test_user"
        password  => "test"
    }
}

