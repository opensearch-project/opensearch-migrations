apiVersion: v2
name: migration-assistant
version: 0.2.0
type: application
appVersion: "latest"
# podFailurePolicy on Jobs requires Kubernetes >= 1.26 (beta) / GA in 1.31.
# Jenkins exercises the chart against Kubernetes 1.34.
kubeVersion: ">=1.34.0-0"
