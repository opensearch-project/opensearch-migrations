apiVersion: v2
name: migration-assistant
version: 0.2.0
type: application
appVersion: "latest"
# ImageVolume is enabled by default in Kubernetes 1.35 and is used by
# image-backed mountable transforms.
kubeVersion: ">=1.35.0-0"
