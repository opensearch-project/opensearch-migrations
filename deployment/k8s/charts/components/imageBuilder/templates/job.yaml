apiVersion: batch/v1
kind: Job
metadata:
  name: image-builder-{{ .Values.migrationServiceName | lower }}
  labels:
    app.kubernetes.io/name: kaniko
    app.kubernetes.io/component: image-builder
    service-name: {{ .Values.migrationServiceName }}
    release-name: {{ .Release.Name }}
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      {{- if .Values.serviceAccountName }}
      serviceAccountName: {{ .Values.serviceAccountName }}
      {{- end }}

      volumes:
        # Mount specified PVC or use local mount
        {{- if .Values.workspaceVolumePvc }}
        - name: workspace-mount
          persistentVolumeClaim:
            claimName: {{ .Values.workspaceVolumePvc }}
        {{- else }}
        - name: workspace-mount
          hostPath:
            path: /opensearch-migrations
            type: DirectoryOrCreate
        {{- end }}

      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:latest
          args:
            - --context=/workspace/{{ .Values.contextDir }}
            - --dockerfile=/workspace/{{ .Values.contextDir }}/Dockerfile
            - --destination={{ .Values.registryDestination }}
            {{- range $key, $value := .Values.buildArgs }}
            - --build-arg={{ $key }}={{ $value }}
            {{- end }}
            - --cache=true
            - --cache-repo={{ .Values.cacheDestination }}
            - --snapshot-mode=redo
            - --cache-copy-layers
            - --compressed-caching=false
            - --use-new-run
            - --insecure
            - --insecure-pull
            - --skip-tls-verify
          volumeMounts:
            - name: workspace-mount
              mountPath: /workspace
              readOnly: true
          resources:
            requests:
              memory: "3Gi"
            limits:
              memory: "6Gi"
