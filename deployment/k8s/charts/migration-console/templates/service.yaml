apiVersion: v1
kind: Service
metadata:
  name: {{ include "migration-console.fullname" . }}
  labels:
    {{- include "migration-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "migration-console.selectorLabels" . | nindent 4 }}
