
pipeline {
    agent { label 'Jenkins-Default-Agent-X64-C5xlarge-Single-Host' }

    stages {
        stage('Load Pipeline') {
            steps {
                script {
                    // Load and execute the pipeline from the helper script
                    def defaultStructure = load 'jenkins/migration-pipelines/defaultStructure.groovy'
                    defaultStructure.call()
                }
            }
        }
    }
}
